// This file can be replaced during build by using the `fileReplacements` array.
// `ng build --prod` replaces `environment.ts` with `environment.prod.ts`.
// The list of file replacements can be found in `angular.json`.

export const environment = {
  production: false,
  firebaseConfig: {
    apiKey: 'AIzaSyCJ3IaIhRoosnu4B331vM1bY9XarKfJu3c',
  authDomain: 'projetformation-ionic.firebaseapp.com',
  projectId: 'projetformation-ionic',
  storageBucket: 'projetformation-ionic.appspot.com',
  messagingSenderId: '164701691181',
  appId: '1:164701691181:web:b4016808a4117ea5b587a4'
  }
};

/*
 * For easier debugging in development mode, you can import the following file
 * to ignore zone related error stack frames such as `zone.run`, `zoneDelegate.invokeTask`.
 *
 * This import should be commented out in production mode because it will have a negative impact
 * on performance if an error is thrown.
 */
// import 'zone.js/dist/zone-error';  // Included with Angular CLI.
